# Requirements

- A node is a running linux process called truthdb.
- The state of a node must be a single file with everything in it.
- A cluster is a set of nodes
- A federation is a set of clusters.
- There must be client libraries that provide an algorithm to select the correct node/cluster to use
  